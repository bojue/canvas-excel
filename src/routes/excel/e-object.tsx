let EXCEL_OBJ = {
    rows:[], // 行
    columns:[], // 列
    datas:[] // 数据对象
}

let EXCLE_DATAS = [
    [
        'start_colum',
        'start_row', 
        'end_colum', 
        'end_row', 
        {
            'fillStyle':null,
            'fontStyle':'normal' || 'italic',
            'fontWeight':'normal' || 'bold',
            'textAlign': 'center' || 'right' || 'left'
        },
        {
            'textLine':'underline'
        }
    ]
]